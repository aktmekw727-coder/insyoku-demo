# 陣力デモ 現状調査レポート（3店舗共通テンプレート化に向けて）

既存の陣力デモ（`index.html` / `style.css` / `script.js` / `assets/`）を直接上書きせず、
鹿児島市の営業候補3店舗に使える「店舗別営業デモの共通テンプレート」へ展開することを目的に、
現状のコードを調査した結果をまとめる。**このレポート作成時点でコードの変更は行っていない。**

## 1. 現在のファイル構成

ビルド不要の静的サイトのみ。外部ライブラリ・フレームワーク・バックエンドなし。

```
insyoku-demo/
├── index.html          (416行 / 1ページ完結のSPA的HTML)
├── style.css           (1157行)
├── script.js           (443行 / 全ロジック・ダミーデータ集約)
├── README.md
├── assets/images/       (11枚のJPG + README.md)
├── dummy                (内容 "x" のみ・用途不明の残骸ファイル)
└── notes.tmp            (内容 "x" のみ・用途不明の残骸ファイル)
```

`dummy` と `notes.tmp` は git log を見ると `506fe3f x` / `cf2300b noop` という空コミットで追加された
1バイトのゴミファイルで、サイト本体からは一切参照されていない（過去のメンテナンス作業の残骸と思われる）。
テンプレート化の際は削除して問題ない候補。

GitHub Actions等のワークフローファイル・CNAME・.nojekyll は存在しない。GitHub Pages設定はリポジトリ設定側
（Settings > Pages）で行われている前提。

## 2. 陣力固有の店名・文章・画像・営業時間・メニューが書かれている場所

| 種別 | ファイル:行 | 内容 |
|---|---|---|
| 店名・タイトル | `index.html:6` | `<title>焼肉 陣力 \| 鹿児島県鹿屋市</title>` |
| ロゴ・ブランド名 | `index.html:76-79` | `KANOYA YAKINIKU` / `焼肉 陣力` |
| 地域バッジ・見出しコピー | `index.html:121-123` | 「鹿児島県鹿屋市」「鹿屋で、家族と。仲間と。」等 |
| 電話番号 | `index.html:256`, `313` | `tel:0994000000` / `0994-00-0000` |
| 所在地 | `index.html:312` | 「鹿児島県鹿屋市（住所はデモ表示用〜）」 |
| 営業時間 | `index.html:187`, `314` | ランチ11:00〜14:00 / 11:00〜14:00, 17:00〜21:30 |
| フッター店名・所在地 | `index.html:392-393` | 「焼肉 陣力」「鹿児島県鹿屋市」 |
| メニュー本体 | `script.js:8-89` | `CATEGORIES` / `RECOMMEND_ITEMS` / `MENU_TABS`（品名・価格・説明文すべて） |
| 診断ロジックの文言 | `script.js:135-179` | `buildRecommendation()` 内の店固有メニュー名（「陣力カルビ」「鹿屋黒豚ばら丼」等）とコメント文 |
| クーポン文言 | `script.js:372` | 「本日のおすすめ一品 100円OFF」 |
| 予約カレンダーのダミー日程 | `script.js:383-389` | `BOOKING_DAYS`（日付・空き状況） |
| 管理画面サンプルの行データ | `index.html:268-270` | 予約経路・状態のサンプル行 |
| 画像ファイル名・キャプション | `index.html`各`<img>`/`data-caption`、`script.js`の`image:`プロパティ、`assets/images/*.jpg` | ファイル名自体は「陣力」名義ではないが、写っている内容（看板等）は陣力固有 |
| カラーテーマ | `style.css:7-29` (`:root`変数) | 木目・えんじ色は「陣力」の店の雰囲気に合わせた配色（店ごとに変えたい場合はここ） |

## 3. 共通化できる部分

- **HTML構造・CSS全体**：セクション構成（ヒーロー/メニュー/ランチ/おすすめ/診断/予約/店舗情報/フッター）、
  SVGアイコン定義（`index.html:12-71`）、レイアウト・レスポンシブCSSはロジック依存がなく、そのまま3店舗で
  共通化可能。
- **script.js のフレームワーク部分**：`renderCategories`, `renderRecommend`, `renderMenuTabs`, `activateTab`,
  診断ウィジェットの進行制御、クーポン開閉、予約カレンダー描画・モーダル制御、口コミ星評価などの
  **関数群（振る舞い）はデータ駆動**で店名に依存していない。店舗固有なのは
  `CATEGORIES`/`RECOMMEND_ITEMS`/`MENU_TABS`/`DIAG_QUESTIONS`の質問文言/`buildRecommendation`内の分岐結果/
  `BOOKING_DAYS`/`COUPON_MESSAGE`といった**データ定義部分**のみ。
- つまり「共通テンプレート化」は、script.js内のデータ定義とindex.html内のテキスト・画像パス・営業時間・
  電話番号を**店舗別設定オブジェクト**に切り出せば、ロジック（診断アルゴリズム含む）とCSSはほぼ手を加えずに
  使い回せる構造。

## 4. 予約カレンダー・予約希望フォーム・管理画面サンプルの実装状況

すべて**フロントエンドのみのモック**で、外部通信・保存は一切ない。

- **予約カレンダー**：`script.js:383-396` の`BOOKING_DAYS`固定配列を5日分描画。状態は「○/△/－」の文字列のみで
  実データ連動なし。
- **予約希望フォーム**：スロットボタンクリックで`#booking-modal`を表示（`index.html:366-387`）。フォーム送信は
  `event.preventDefault()`で止め（`script.js:419-423`）、「送信完了」表示に切り替えるだけ。送信内容はどこにも
  保存・送信されない。
- **管理画面サンプル**：`index.html:260-272`の`admin-board`は静的なハードコード行（3行）で、インタラクション
  なし・実際の管理機能はない「見せるだけ」のUIモック。

いずれも「本番化する場合はこう拡張する」というデモ用の説明的UIであり、実装としては状態を持たないハリボテ。
3店舗展開でもこの位置づけ（モック）を維持するのが自然。

## 5. 3店舗分を安全に分けるための構成案

既存の陣力デモを壊さないことが条件のため、**既存ファイルは一切変更せず、新規ディレクトリで複製→データ層を
切り出す**方針を推奨する。

```
insyoku-demo/                 ← 既存(陣力)は現状維持、または将来 stores/jinriki/ に移設
├── index.html / style.css / script.js / assets/   (既存・変更しない)
└── stores/                                          ← 新設
    ├── shared/
    │   ├── style.css        (陣力style.cssから店名固有色以外を共通化 or そのまま複製)
    │   └── app.js           (renderXxx関数・診断エンジン・予約UI等の“ロジック”のみ)
    ├── jinriki/              (既存デモの複製、テンプレート適用の動作確認用)
    │   ├── index.html
    │   ├── config.js         (店名・電話番号・営業時間・メニュー・画像パス等)
    │   └── assets/images/
    ├── store-b/
    │   ├── index.html
    │   ├── config.js
    │   └── assets/images/
    └── store-c/
        ├── index.html
        ├── config.js
        └── assets/images/
```

- 各店舗の`config.js`に「2.」で列挙した店固有情報（店名/電話/住所/営業時間/カラー変数/`CATEGORIES`/
  `RECOMMEND_ITEMS`/`MENU_TABS`/`BOOKING_DAYS`/クーポン文言等）をまとめ、`shared/app.js`はそのconfigを
  受け取って描画するだけにする。
- 画像は店舗ごとに`assets/images/`配下を分け、ファイル名は共通命名（`hero.jpg`等）にしておくとconfigの
  差し替えだけで済む。
- 既存の`insyoku-demo/index.html`等はURLも内容も変えずそのまま残すため、既存デモへの影響ゼロで並行運用できる。

## 6. GitHub Pagesで各店舗デモを別URLで表示する方法

このリポジトリにはPages用ワークフローがなく、リポジトリ設定側でPagesが有効化されている想定（設定内容は
このツールからは確認できないため、実際の設定はGitHub側で確認が必要）。一般的な選択肢は以下の3つ。

- **案A：サブディレクトリ分け（推奨・最も安全）**
  同一リポジトリ・同一Pagesサイト内で `https://<user>.github.io/insyoku-demo/stores/jinriki/`、
  `.../stores/store-b/` のようにパスを分ける。Pages設定は今のまま（ブランチ配信）でよく、既存の
  `https://<user>.github.io/insyoku-demo/`（陣力トップ）にも影響しない。
- **案B：別リポジトリに分割**
  店舗ごとにリポジトリを作り、それぞれ独立したPages URL（`store-b-demo.github.io`等）にする。営業デモとして
  「1店舗1URL」を強く見せたい場合に有効だが、共通テンプレートの更新を3リポジトリに反映する手間が増える。
- **案C：ブランチ別Pages**
  ブランチごとにPages環境を切る方法もあるが、GitHub Pagesは基本1リポジトリ1サイト（Pages自体は
  organizationプランでカスタム設定も可能）なので、複数ブランチを別URLで同時公開するには追加設定（Actions等）
  が必要になり、複雑さが増す。

このリポジトリ構成・目的（営業候補への見せ方）であれば**案Aが最も手間が少なく既存デモを壊すリスクも低い**と
考える。

## 7. 変更時に陣力デモを壊す可能性がある箇所

- **`index.html` / `style.css` / `script.js` を直接編集すること自体**：現状の陣力デモの唯一の実体なので、
  これらを直接書き換えると即座に陣力デモが変わってしまう。テンプレート化は新規ファイル・新規ディレクトリで
  行い、既存3ファイルには触れないことが最優先。
- **`assets/images/` の共有**：もし店舗別ディレクトリで同じ`assets/images/`を参照する実装にすると、画像を
  差し替えた瞬間に陣力デモの見た目も変わってしまう。画像ディレクトリは店舗ごとに完全に分離する必要がある。
- **`script.js`内のグローバルなデータ定義・DOM ID依存**：`CATEGORIES`, `MENU_TABS`, `BOOKING_DAYS`等は
  トップレベルの`const`で、DOM要素IDも`document.getElementById("...")`という固定文字列に依存している
  （例：`booking-calendar`, `coupon-text`, `star-select`等）。これを「共通ロジック化」する際に、複数店舗を
  同じHTML内やロード順に混在させると、ID重複やグローバル変数の競合で陣力デモの動作が壊れる可能性がある。
  店舗ごとにページ（HTMLファイル）を完全に分離すればこのリスクは回避できる。
- **CSSの`:root`カラー変数の書き換え**：`style.css:7-21`の配色は陣力の世界観に合わせたもの。共通CSSとして
  流用する際にここを直接上書きすると陣力デモの配色も変わるため、店舗別に別ファイル（または別`:root`スコープ）
  で管理する必要がある。
- **`README.md`・`assets/images/README.md`の記述**：現状は陣力専用の説明になっているため、共通テンプレート化
  に伴い記述を変える場合は、既存デモの説明として矛盾が出ないよう注意が必要（ただし機能に影響はしない）。
- **`dummy` / `notes.tmp`**：機能的には無害なゴミファイルだが、削除する場合も陣力デモの動作には影響しない
  （未参照のため）。

---

以上が調査結果の全文。次のステップ（テンプレート化の実装方針の確定・着手）は別途指示を待つ。
